apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: caps-ai-backend-canary-ingress
  namespace: caps-ai
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/canary: "true"
    nginx.ingress.kubernetes.io/canary-weight: "0"
spec:
  rules:
    - host: caps-ai.example.com
      http:
        paths:
          - path: /api/v1
            pathType: Prefix
            backend:
              service:
                name: backend-canary
                port:
                  number: 8000
